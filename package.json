{
  "name": "prseo-astro",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "validate": "node scripts/validate-data.mjs",
    "build": "npm run validate && astro check && astro build",
    "deploy": "wrangler deploy --assets=./dist",
    "preview": "astro preview",
    "astro": "astro",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "astro": "^5.1.5"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.4",
    "@astrojs/sitemap": "^3.7.0",
    "@rollup/rollup-linux-arm64-gnu": "^4.57.1",
    "typescript": "^5.7.3",
    "vitest": "^2.1.0"
  }
}
